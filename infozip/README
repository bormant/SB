This is infozip slackbuild script from Slackware.

Added patch to build infozip with libnatspec.

To download sources use:

  # . *info
  # wget $DOWNLOAD

To build with libnatspec use:

  # NATSPEC=yes sh *Build
